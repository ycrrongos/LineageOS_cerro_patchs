project packages/apps/Launcher3/
diff --git a/packages/apps/Launcher3/quickstep/src/com/android/launcher3/taskbar/NavbarButtonsViewController.java b/packages/apps/Launcher3/quickstep/src/com/android/launcher3/taskbar/NavbarButtonsViewController.java
index bd2fe9a..c537dab 100644
--- a/packages/apps/Launcher3/quickstep/src/com/android/launcher3/taskbar/NavbarButtonsViewController.java
+++ b/packages/apps/Launcher3/quickstep/src/com/android/launcher3/taskbar/NavbarButtonsViewController.java
@@ -321,6 +321,9 @@ public class NavbarButtonsViewController implements TaskbarControllers.LoggableT
         navButtonsViewLayoutParams.width = p.x;
         navButtonsViewLayoutParams.height = p.y;
         mNavButtonsView.setLayoutParams(navButtonsViewLayoutParams);
+        // On connected displays, redirecting taps to the nearest nav button can steal touches
+        // from taskbar icons rendered underneath the nav button layer.
+        mNavButtonsView.setIsActiveOverride(mContext.isPrimaryDisplay());
 
         mIsImeRenderingNavButtons = mContext.imeDrawsImeNavBar();
         if (!mIsImeRenderingNavButtons) {
diff --git a/packages/apps/Launcher3/quickstep/src/com/android/launcher3/taskbar/navbutton/NearestTouchFrame.java b/packages/apps/Launcher3/quickstep/src/com/android/launcher3/taskbar/navbutton/NearestTouchFrame.java
index 844f1af..e4e58c4 100644
--- a/packages/apps/Launcher3/quickstep/src/com/android/launcher3/taskbar/navbutton/NearestTouchFrame.java
+++ b/packages/apps/Launcher3/quickstep/src/com/android/launcher3/taskbar/navbutton/NearestTouchFrame.java
@@ -43,6 +43,7 @@ public class NearestTouchFrame extends FrameLayout {
     private final List<View> mClickableChildren = new ArrayList<>();
     private final List<View> mAttachedChildren = new ArrayList<>();
     private final boolean mIsActive;
+    private boolean mIsActiveOverride = true;
     private final int[] mTmpInt = new int[2];
 
     // Offset (as the base) to translate window cords to view cords.
@@ -74,6 +75,13 @@ public class NearestTouchFrame extends FrameLayout {
         mIsActive = c.smallestScreenWidthDp < 600;
     }
 
+    /**
+     * Enables or disables nearest-button touch redirection.
+     */
+    public void setIsActiveOverride(boolean isActiveOverride) {
+        mIsActiveOverride = isActiveOverride;
+    }
+
     @Override
     protected void onLayout(boolean changed, int left, int top, int right, int bottom) {
         super.onLayout(changed, left, top, right, bottom);
@@ -177,7 +185,7 @@ public class NearestTouchFrame extends FrameLayout {
 
     @Override
     public boolean onTouchEvent(MotionEvent event) {
-        if (mIsActive) {
+        if (mIsActive && mIsActiveOverride) {
             int x = (int) event.getX();
             int y = (int) event.getY();
             if (event.getAction() == MotionEvent.ACTION_DOWN) {
@@ -206,6 +214,7 @@ public class NearestTouchFrame extends FrameLayout {
 
         pw.println(String.format("%s\tmWindowOffset=%s", prefix, Arrays.toString(mWindowOffset)));
         pw.println(String.format("%s\tmIsVertical=%s", prefix, mIsVertical));
+        pw.println(String.format("%s\tmIsActiveOverride=%s", prefix, mIsActiveOverride));
         pw.println(String.format("%s\tmTouchingChild=%s", prefix, mTouchingChild));
         pw.println(String.format("%s\tmTouchableRegions=%s", prefix,
                 mTouchableRegions.keySet().stream()

project frameworks/base/
diff --git a/frameworks/base/libs/WindowManager/Shell/src/com/android/wm/shell/desktopmode/DesktopTasksController.kt b/frameworks/base/libs/WindowManager/Shell/src/com/android/wm/shell/desktopmode/DesktopTasksController.kt
index b6fd5fc45..afa0fbc71 100644
--- a/frameworks/base/libs/WindowManager/Shell/src/com/android/wm/shell/desktopmode/DesktopTasksController.kt
+++ b/frameworks/base/libs/WindowManager/Shell/src/com/android/wm/shell/desktopmode/DesktopTasksController.kt
@@ -2500,11 +2500,51 @@ class DesktopTasksController(
         displayId: Int,
         userId: Int = shellController.currentUserId,
     ) {
-        val repository = userRepositories.getProfile(userId)
-        val wct = WindowContainerTransaction()
         val displayLayout = displayController.getDisplayLayout(displayId) ?: return
         val bounds = calculateDefaultDesktopTaskBounds(displayLayout)
-        val deskId = getOrCreateDefaultDeskId(displayId, userId) ?: return
+        if (
+            !DesktopExperienceFlags.ENABLE_MULTIPLE_DESKTOPS_BACKEND.isTrue ||
+                !DesktopExperienceFlags.ENABLE_DEFAULT_DESK_WITHOUT_WARMUP_MIGRATION.isTrue
+        ) {
+            startLaunchIntentTransitionInternal(
+                intent = intent,
+                options = options,
+                displayId = displayId,
+                userId = userId,
+                displayLayout = displayLayout,
+                bounds = bounds,
+                deskId = getOrCreateDefaultDeskId(displayId, userId) ?: return,
+            )
+            return
+        }
+        mainScope.launch {
+            try {
+                startLaunchIntentTransitionInternal(
+                    intent = intent,
+                    options = options,
+                    displayId = displayId,
+                    userId = userId,
+                    displayLayout = displayLayout,
+                    bounds = bounds,
+                    deskId = getOrCreateDefaultDeskIdSuspending(displayId, userId),
+                )
+            } catch (t: Throwable) {
+                logE("Failed to start launch intent transition: %s", t.message)
+            }
+        }
+    }
+
+    private fun startLaunchIntentTransitionInternal(
+        intent: Intent,
+        options: Bundle,
+        displayId: Int,
+        userId: Int,
+        displayLayout: DisplayLayout,
+        bounds: Rect,
+        deskId: Int,
+    ) {
+        val repository = userRepositories.getProfile(userId)
+        val wct = WindowContainerTransaction()
         if (DesktopModeFlags.ENABLE_CASCADING_WINDOWS.isTrue) {
             val stableBounds = Rect().also { displayLayout.getStableBounds(it) }
             cascadeWindow(
diff --git a/frameworks/base/services/core/java/com/android/server/display/DisplayTopologyCoordinator.java b/frameworks/base/services/core/java/com/android/server/display/DisplayTopologyCoordinator.java
index ac6c4c7fa..78683ffe8 100644
--- a/frameworks/base/services/core/java/com/android/server/display/DisplayTopologyCoordinator.java
+++ b/frameworks/base/services/core/java/com/android/server/display/DisplayTopologyCoordinator.java
@@ -301,11 +301,18 @@ class DisplayTopologyCoordinator {
         if (info == null) {
             return false;
         }
+        final boolean isTrustedPublicSystemDecorVirtualDisplay =
+                info.type == Display.TYPE_VIRTUAL
+                        && (info.flags & Display.FLAG_TRUSTED) != 0
+                        && (info.flags & Display.FLAG_PRIVATE) == 0
+                        && (info.flags & Display.FLAG_SHOULD_SHOW_SYSTEM_DECORATIONS) != 0;
         if (info.type != Display.TYPE_INTERNAL && info.type != Display.TYPE_EXTERNAL
-                && info.type != Display.TYPE_OVERLAY) {
+                && info.type != Display.TYPE_OVERLAY
+                && !isTrustedPublicSystemDecorVirtualDisplay) {
             if (shouldLog) {
                 Slog.d(TAG, "Display " + info.displayId + " not allowed in topology because "
-                        + "type is not INTERNAL, EXTERNAL or OVERLAY");
+                        + "type is not INTERNAL, EXTERNAL, OVERLAY or trusted public system-decor"
+                        + " VIRTUAL");
             }
             return false;
         }
diff --git a/frameworks/base/services/core/java/com/android/server/wm/DisplayContent.java b/frameworks/base/services/core/java/com/android/server/wm/DisplayContent.java
index 27427724f..a12adb98a 100644
--- a/frameworks/base/services/core/java/com/android/server/wm/DisplayContent.java
+++ b/frameworks/base/services/core/java/com/android/server/wm/DisplayContent.java
@@ -5805,7 +5805,13 @@ class DisplayContent extends RootDisplayArea implements WindowManagerPolicy.Disp
             return false;
         }
         return switch (windowingMode) {
-            case WINDOWING_MODE_FREEFORM -> mAtmService.mSupportsFreeformWindowManagement;
+            case WINDOWING_MODE_FREEFORM ->
+                    mAtmService.mSupportsFreeformWindowManagement
+                            || (mDisplay.getType() == Display.TYPE_VIRTUAL
+                            && isTrusted()
+                            && !isPrivate()
+                            && (mDisplay.getFlags()
+                            & FLAG_SHOULD_SHOW_SYSTEM_DECORATIONS) != 0);
             case WINDOWING_MODE_PINNED -> mAtmService.mSupportsPictureInPicture;
             case WINDOWING_MODE_MULTI_WINDOW -> mAtmService.mSupportsMultiWindow;
             default -> true;
diff --git a/frameworks/base/services/core/java/com/android/server/wm/DisplayWindowSettings.java b/frameworks/base/services/core/java/com/android/server/wm/DisplayWindowSettings.java
index 9004a6d4a..56ce010a0 100644
--- a/frameworks/base/services/core/java/com/android/server/wm/DisplayWindowSettings.java
+++ b/frameworks/base/services/core/java/com/android/server/wm/DisplayWindowSettings.java
@@ -257,6 +257,15 @@ class DisplayWindowSettings {
             // Default display should show system decors.
             return true;
         }
+        if (dc.getDisplay().getType() == Display.TYPE_VIRTUAL
+                && dc.isTrusted()
+                && !dc.isPrivate()
+                && (dc.getDisplay().getFlags()
+                & Display.FLAG_SHOULD_SHOW_SYSTEM_DECORATIONS) != 0) {
+            // Trusted public virtual displays may advertise fixed system decorations without
+            // participating in content mode switching. Treat them as topology participants.
+            return true;
+        }
 
         final DisplayInfo displayInfo = dc.getDisplayInfo();
         final SettingsProvider.SettingsEntry settings = mSettingsProvider.getSettings(displayInfo);
diff --git a/frameworks/base/services/core/java/com/android/server/wm/TaskDisplayArea.java b/frameworks/base/services/core/java/com/android/server/wm/TaskDisplayArea.java
index 9d005fc7b..374ae0db7 100644
--- a/frameworks/base/services/core/java/com/android/server/wm/TaskDisplayArea.java
+++ b/frameworks/base/services/core/java/com/android/server/wm/TaskDisplayArea.java
@@ -1268,6 +1268,14 @@ final class TaskDisplayArea extends DisplayArea<WindowContainer> {
         }
 
         if (!supportsFreeform && windowingMode == WINDOWING_MODE_FREEFORM) {
+            if (mDisplayContent != null
+                    && mDisplayContent.getDisplay().getType() == android.view.Display.TYPE_VIRTUAL
+                    && mDisplayContent.isTrusted()
+                    && !mDisplayContent.isPrivate()
+                    && (mDisplayContent.getDisplay().getFlags()
+                    & android.view.Display.FLAG_SHOULD_SHOW_SYSTEM_DECORATIONS) != 0) {
+                return true;
+            }
             return false;
         }
 
diff --git a/frameworks/base/services/core/java/com/android/server/wm/WindowManagerService.java b/frameworks/base/services/core/java/com/android/server/wm/WindowManagerService.java
index dd4c99249..d0e6e00f5 100644
--- a/frameworks/base/services/core/java/com/android/server/wm/WindowManagerService.java
+++ b/frameworks/base/services/core/java/com/android/server/wm/WindowManagerService.java
@@ -8037,13 +8037,26 @@ public class WindowManagerService extends IWindowManager.Stub
                         + "for a display that does not exist: %d", displayId);
                 return false;
             }
-            if (!displayContent.isSystemDecorationsSupported()) {
+            final boolean systemDecorSupported = displayContent.isSystemDecorationsSupported();
+            final boolean freeformSupported =
+                    displayContent.isWindowingModeSupported(WINDOWING_MODE_FREEFORM);
+            final boolean defaultDisplay = displayContent.isDefaultDisplay;
+            final boolean allowContentModeSwitch = displayContent.allowContentModeSwitch();
+            final boolean trustedVirtualDisplay = displayContent.getDisplay().getType()
+                    == Display.TYPE_VIRTUAL
+                    && displayContent.isTrusted()
+                    && !displayContent.isPrivate()
+                    && (displayContent.getDisplay().getFlags()
+                    & Display.FLAG_SHOULD_SHOW_SYSTEM_DECORATIONS) != 0;
+            if (!systemDecorSupported || !freeformSupported) {
                 return false;
             }
-            if (!displayContent.isWindowingModeSupported(WINDOWING_MODE_FREEFORM)) {
-                return false;
+            if (defaultDisplay || allowContentModeSwitch) {
+                return true;
             }
-            return displayContent.isDefaultDisplay || displayContent.allowContentModeSwitch();
+            // Trusted public virtual displays may opt into desktop mode by always showing their
+            // own system decorations instead of participating in content mode switching.
+            return trustedVirtualDisplay;
         }
     }
diff --git a/frameworks/base/build/release/aconfig/ap2a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto b/frameworks/base/build/release/aconfig/ap2a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
new file mode 100644
--- /dev/null
+++ b/frameworks/base/build/release/aconfig/ap2a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
@@ -0,0 +1,5 @@
+flag_value {
+  package: "com.android.window.flags"
+  name: "default_desk_without_warmup_migration"
+  state: ENABLED
+  permission: READ_ONLY
+}
diff --git a/frameworks/base/build/release/aconfig/ap3a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto b/frameworks/base/build/release/aconfig/ap3a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
new file mode 100644
--- /dev/null
+++ b/frameworks/base/build/release/aconfig/ap3a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
@@ -0,0 +1,5 @@
+flag_value {
+  package: "com.android.window.flags"
+  name: "default_desk_without_warmup_migration"
+  state: ENABLED
+  permission: READ_ONLY
+}
diff --git a/frameworks/base/build/release/aconfig/ap4a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto b/frameworks/base/build/release/aconfig/ap4a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
new file mode 100644
--- /dev/null
+++ b/frameworks/base/build/release/aconfig/ap4a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
@@ -0,0 +1,5 @@
+flag_value {
+  package: "com.android.window.flags"
+  name: "default_desk_without_warmup_migration"
+  state: ENABLED
+  permission: READ_ONLY
+}
diff --git a/frameworks/base/build/release/aconfig/bp1a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto b/frameworks/base/build/release/aconfig/bp1a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
new file mode 100644
--- /dev/null
+++ b/frameworks/base/build/release/aconfig/bp1a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
@@ -0,0 +1,5 @@
+flag_value {
+  package: "com.android.window.flags"
+  name: "default_desk_without_warmup_migration"
+  state: ENABLED
+  permission: READ_ONLY
+}
diff --git a/frameworks/base/build/release/aconfig/bp2a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto b/frameworks/base/build/release/aconfig/bp2a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
new file mode 100644
--- /dev/null
+++ b/frameworks/base/build/release/aconfig/bp2a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
@@ -0,0 +1,5 @@
+flag_value {
+  package: "com.android.window.flags"
+  name: "default_desk_without_warmup_migration"
+  state: ENABLED
+  permission: READ_ONLY
+}
diff --git a/frameworks/base/build/release/aconfig/bp3a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto b/frameworks/base/build/release/aconfig/bp3a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
new file mode 100644
--- /dev/null
+++ b/frameworks/base/build/release/aconfig/bp3a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
@@ -0,0 +1,5 @@
+flag_value {
+  package: "com.android.window.flags"
+  name: "default_desk_without_warmup_migration"
+  state: ENABLED
+  permission: READ_ONLY
+}
diff --git a/frameworks/base/build/release/aconfig/bp4a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto b/frameworks/base/build/release/aconfig/bp4a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
new file mode 100644
--- /dev/null
+++ b/frameworks/base/build/release/aconfig/bp4a/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
@@ -0,0 +1,5 @@
+flag_value {
+  package: "com.android.window.flags"
+  name: "default_desk_without_warmup_migration"
+  state: ENABLED
+  permission: READ_ONLY
+}
diff --git a/frameworks/base/build/release/aconfig/trunk_staging/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto b/frameworks/base/build/release/aconfig/trunk_staging/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
new file mode 100644
--- /dev/null
+++ b/frameworks/base/build/release/aconfig/trunk_staging/com.android.window.flags/default_desk_without_warmup_migration_flag_values.textproto
@@ -0,0 +1,5 @@
+flag_value {
+  package: "com.android.window.flags"
+  name: "default_desk_without_warmup_migration"
+  state: ENABLED
+  permission: READ_ONLY
+}
